Q.1.write program to calculate length of string using standard library 

#include<Stdio.h>
int main()
{
char s1[20];
int k;
printf("enter string");
gets(s1);
k=strlen(s1);
printf("length of string=%d",k);
}

Q.2. Write a program to allocate memory dynamically for n integers Accept the elements and calculate their sum and average

#include<stdio.h>
int main()
{
int i,n,*p,s=0;
float avg;
printf("enter limit");
scanf("%d",&n);
p=(int *)malloc(sizeof(int));
printf("enter n numbers");
for(i=0;i<n;i++)
{
scanf("%d",(p+i);
}
for(i=0;i<n;i++)
{
s=s+*(p+i);
}
avg=s/n;
printf("\n sum=%d",s);
printf("\n average=%f",avg);
}


